import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  // Descomentar React.StrictMode para fins de desenvolvimento
  // <React.StrictMode> 
    <App />
  // </React.StrictMode>
);